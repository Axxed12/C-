#include <stdio.h>
double Max(double a,double b)
{
  if(a>b)
  return a;
  else
  return b;
}
int main()
{
	double a;
	double b;
	scanf_s("%lf %lf", &a,&b);
  double m=Max(a,b);
  printf("%f",m);
	return 0;

}
